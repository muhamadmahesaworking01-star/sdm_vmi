# Arsitektur Aplikasi

Laravel MVC: route dan middleware mengatur akses, controller menjalankan use case, model Eloquent mengakses database, dan Blade menyajikan dashboard. `LogActivity` dipasang pada grup web untuk audit terpusat; `BackupController` menangani ekspor snapshot.
