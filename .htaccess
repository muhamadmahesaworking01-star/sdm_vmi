<IfModule mod_rewrite.c>
    RewriteEngine On

    # Arahkan seluruh request aplikasi ke document root Laravel.
    RewriteCond %{REQUEST_URI} !^/public(?:/|$)
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
